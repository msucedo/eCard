{
  "name": "eCard",
  "packageManager": "yarn@3.1.1",
  "private": true,
  "workspaces": [
    "hardhat-zone",
    "react-zone"
  ],
  "scripts": {
    "chain": "yarn workspace hardhat-zone chain",
    "compile": "yarn workspace hardhat-zone compile",
    "deploy": "yarn workspace hardhat-zone deploy",
    "start": "yarn workspace react-zone start"
  }
}
